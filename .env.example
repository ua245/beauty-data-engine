# Vercel (frontend)
# Leave empty to use the /api proxy in production; set for direct backend access.
VITE_API_BASE=/api/v1

# Vercel serverless proxy (api/[...path].js)
# URL of the deployed Rust backend (no trailing slash)
BACKEND_URL=https://your-rust-backend.example.com
